{
  "name": "my-webapp-app",
  "version": "1.0.0",
  "description": "PWA to Android App using Capacitor",
  "scripts": {
    "init-capacitor": "cap init",
    "sync-capacitor": "cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
